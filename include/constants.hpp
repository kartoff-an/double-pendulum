#pragma once
#include <SFML/Graphics.hpp>

namespace COLOR {
    inline constexpr sf::Color DARK_GRAY = sf::Color(50, 50, 50);
    inline constexpr sf::Color GRAY = sf::Color(128, 128, 128, 150);
    inline constexpr sf::Color FAINT_WHITE = sf::Color(150, 150, 150);
    inline constexpr sf::Color RED = sf::Color::Red;
    inline constexpr sf::Color BLUE = sf::Color::Blue;
}